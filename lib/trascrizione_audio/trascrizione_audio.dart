import 'package:flutter/material.dart';

class TrascrizioneAudioScreen extends StatelessWidget {
  const TrascrizioneAudioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}